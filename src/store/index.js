import Vue from 'vue';
import Vuex from 'vuex';
import { ENV } from '@/config';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
  strict: ENV !== 'production'
});
